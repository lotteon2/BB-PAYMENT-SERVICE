server:
  port: 8100
spring:
  application:
    name: payment-service
  config:
    activate:
      on-profile: local, dev, local
    import: optional:configserver:http://localhost:8888
management:
  endpoints:
    web:
      exposure:
        include:
          - "refresh"
          - "bus-refresh"